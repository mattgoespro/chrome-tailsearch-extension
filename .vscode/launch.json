{
  "configurations": [
    {
      "name": "debug",
      "type": "node",
      "request": "attach",
      "sourceMaps": true,
      "sourceMapRenames": true,
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "debug",
      // map sourcemaps from `dist/js/*.js` to `src`
      "resolveSourceMapLocations": ["${workspaceFolder}/dist/**/*.map.js"]
    }
  ]
}
